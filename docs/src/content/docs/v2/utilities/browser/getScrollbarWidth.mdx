---
title: getScrollbarWidth
description: A function that returns the width of the vertical scrollbar.
---

import { Badge } from '@astrojs/starlight/components';

<Badge text="Type" />: `function`

A function that returns the width of the vertical scrollbar.

## Usage
```ts
import { getScrollbarWidth } from '@mustib/utils/browser';

const scrollbarWidth = getScrollbarWidth();
console.log(scrollbarWidth);
```