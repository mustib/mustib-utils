---
title: wait
description: A function that returns a promise that resolves after a specified amount of time.
---

import {
  TypeSince,
  Returns,
  Parameters
} from '../../components'

<TypeSince since='2.4.0' type='function'/>

The **`wait`** function returns a promise that resolves after a specified amount of time. It is useful for adding a delay to your code.

## Usage
```ts
import { wait } from '@mustib/utils';

await wait(1000); // wait for 1 second
```

## Definition
```ts
function wait(ms: number): Promise<void> { }
```

<Parameters parameters={[
  {
    name: 'ms',
    type: 'number',
    description: [
      'The number of milliseconds to wait.'
    ]
  }
]}/>

<Returns type='Promise<void>'/>
