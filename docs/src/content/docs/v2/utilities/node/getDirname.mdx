---
title: getDirname
description: Resolve __dirname in ES modules from import.meta.url.
---

import {
  Type,
  Parameters,
  Returns,
} from '../../components'

<Type type='function' />

The **`getDirname`** function resolves the directory path of a module file in ESM environments.

Use it when you need `__dirname`-like behavior with `import.meta.url`.

## Usage
```ts
import { getDirname } from '@mustib/utils/node';

const __dirname = getDirname(import.meta.url);
```

## Definition
```ts
function getDirname(metaUrl: string): string { }
```

<Parameters parameters={[
  {
    name: 'metaUrl',
    type: 'string',
    description: [
      'Usually the value of `import.meta.url` from the current module.',
    ]
  }
]} />

<Returns type='string' details={[
  'Absolute directory path for the provided module URL.',
]}/>
